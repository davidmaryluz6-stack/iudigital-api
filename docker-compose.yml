services:
  mongo:
    image: mongo:6
    container_name: mongo_iudigital
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  monolito-api:
    build:
      context: ./monolito-api
    container_name: monolito_api
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGO_URI=mongodb://mongo:27017/iudigital
    depends_on:
      - mongo

  ms-proyectos:
    build:
      context: ./ms-proyectos
    container_name: ms_proyectos
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - MONGO_URI=mongodb://mongo:27017/iudigital
    depends_on:
      - mongo

volumes:
  mongo_data: